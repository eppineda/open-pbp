import Character from './public/character.js'

/*
 * todo: This part should be generated
 */
const c1 = new Character('Conan')
const c2 = new Character('Terminator')
const characters = [ c1, c2 ]

export default characters
